import {Text} from 'react-native';
import {P1LargeStyles} from '../../../themes/styles';

const P1Large = ({children}) => (
  <Text style={P1LargeStyles.textStyle}>{children}</Text>
);

export default P1Large;
